name: Node.js CD
on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Deploy using ssh
      uses: phamha98/node1@master
      run: |
        echo "Hello, world!"
          
